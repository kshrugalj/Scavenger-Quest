CREATE Table AreasOfInterest(
    id BIGINT,
    top_latitude FLOAT,
    bottom_latitude FLOAT,
    left_longitude FLOAT,
    right_longitude FLOAT

);

CREATE Table KeyLocations(CREATE Table KeyLocations(
    id BIGINT,
    location_id BIGINT,
    latitude BIGINT,
    longitude BIGINT
);
    id BIGINT,
    location_id BIGINT,
    latitude BIGINT,
    longitude BIGINT
);